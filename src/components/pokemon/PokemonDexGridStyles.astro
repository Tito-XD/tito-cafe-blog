<style is:global>
	.p151-toolbar {
		display: flex;
		flex-wrap: wrap;
		align-items: stretch;
		gap: 0.5rem;
		margin-bottom: 0.55rem;
	}

	.p151-toolbar__toggle {
		flex: 0 0 auto;
		padding: 0.55rem 0.85rem;
		border-radius: var(--radius-soft);
		border: 1px solid color-mix(in srgb, var(--color-border) 88%, transparent);
		background: var(--color-surface);
		color: var(--color-text);
		font-weight: 600;
		font-size: 0.88rem;
		line-height: 1.2;
		cursor: pointer;
		transition:
			background var(--motion-gentle),
			border-color var(--motion-gentle),
			color var(--motion-gentle);
	}

	.p151-toolbar__toggle:hover,
	.p151-toolbar__toggle:focus-visible {
		border-color: color-mix(in srgb, var(--color-accent) 38%, var(--color-border) 62%);
	}

	.p151-toolbar__toggle.is-active {
		border-color: color-mix(in srgb, var(--color-accent) 72%, var(--color-border) 28%);
		background: color-mix(in srgb, var(--color-accent) 14%, var(--color-surface) 86%);
		color: color-mix(in srgb, var(--color-accent) 82%, var(--color-text) 18%);
	}

	.p151-search {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		flex: 1 1 12rem;
		width: auto;
		min-width: 0;
		padding: 0.55rem 0.85rem;
		border-radius: var(--radius-soft);
		border: 1px solid color-mix(in srgb, var(--color-border) 88%, transparent);
		background: var(--color-surface);
	}

	.p151-search input {
		flex: 1;
		min-width: 0;
		border: none;
		background: transparent;
		color: var(--color-text);
		font-size: 0.95rem;
		outline: none;
	}

	.p151-search__icon {
		width: 1.1rem;
		height: 1.1rem;
		color: var(--color-muted);
	}

	.p151-grid {
		display: flex;
		flex-wrap: wrap;
		gap: 0.55rem;
		margin: 0;
		padding: 0;
		list-style: none;
		align-items: stretch;
	}

	.p151-evoline {
		position: relative;
		display: flex;
		flex-direction: row;
		flex-wrap: wrap;
		align-items: stretch;
		flex: 0 0 auto;
		gap: 0.1rem 0.12rem;
		padding: 0.5rem 0.4rem 0.4rem;
		border-radius: var(--radius-soft);
		border: 1px solid color-mix(in srgb, var(--color-border) 86%, transparent);
		border-top: 2px solid color-mix(in srgb, var(--color-accent) 48%, var(--color-border) 52%);
		background: color-mix(in srgb, var(--color-surface) 94%, var(--color-accent-soft) 6%);
		max-width: 100%;
		box-shadow: inset 0 1px 0 color-mix(in srgb, var(--color-accent) 12%, transparent);
	}

	.p151-evoline__sep {
		flex: 0 0 auto;
		align-self: center;
		color: var(--color-muted);
		font-size: 0.68rem;
		opacity: 0.45;
		line-height: 1;
	}

	.p151-evoline--typed {
		border-color: color-mix(in srgb, var(--p151-chain-type) 38%, var(--color-border) 62%);
		border-top-color: color-mix(in srgb, var(--p151-chain-type) 58%, var(--color-border) 42%);
		background: color-mix(in srgb, var(--p151-chain-type) 14%, var(--color-surface) 86%);
		box-shadow: inset 0 1px 0 color-mix(in srgb, var(--p151-chain-type) 18%, transparent);
	}

	.p151-member {
		flex: 0 0 5.6rem;
		width: 5.6rem;
		min-width: 5.6rem;
		display: grid;
		gap: 0.15rem;
		padding: 0.28rem 0.2rem 0.32rem;
		text-decoration: none;
		color: inherit;
		text-align: center;
		border: none;
		border-radius: 0.32rem;
		background: transparent;
		transition:
			background var(--motion-gentle),
			border-color var(--motion-gentle);
	}

	.p151-member--card {
		box-sizing: border-box;
		border-radius: var(--radius-soft);
		width: var(--p151-card-width, 5.85rem);
		min-width: var(--p151-card-width, 5.85rem);
		max-width: var(--p151-card-width, 5.85rem);
		min-height: var(--p151-card-min-height, 9.15rem);
		flex: 0 0 var(--p151-card-width, 5.85rem);
	}

	.p151-member--card.p151-member--typed {
		border: 1px solid color-mix(in srgb, var(--p151-chain-type) 38%, var(--color-border) 62%);
		border-top: 2px solid color-mix(in srgb, var(--p151-chain-type) 58%, var(--color-border) 42%);
		background: color-mix(in srgb, var(--p151-chain-type) 14%, var(--color-surface) 86%);
		box-shadow: inset 0 1px 0 color-mix(in srgb, var(--p151-chain-type) 18%, transparent);
	}

	.p151-member--card.p151-member--typed:hover,
	.p151-member--card.p151-member--typed:focus-visible {
		border-color: color-mix(in srgb, var(--p151-chain-type) 48%, var(--color-border) 52%);
		border-top-color: color-mix(in srgb, var(--p151-chain-type) 68%, var(--color-border) 32%);
		background: color-mix(in srgb, var(--p151-chain-type) 22%, var(--color-surface) 78%);
	}

	.pokedex-chain-group {
		list-style: none;
		flex: 0 1 auto;
		min-width: 0;
		max-width: 100%;
	}

	.pokedex-chain-group__track {
		display: flex;
		flex-wrap: wrap;
		align-items: stretch;
		gap: 0.28rem;
	}

	.pokedex-chain__sep {
		flex: 0 0 auto;
		align-self: center;
		color: var(--color-muted);
		font-size: 0.68rem;
		opacity: 0.45;
		line-height: 1;
		padding: 0 0.05rem;
	}

	.pokedex-flat-cell {
		list-style: none;
		display: flex;
		align-items: stretch;
		min-width: 0;
		min-height: var(--p151-card-min-height, 9.15rem);
	}

	.pokedex-grid--flat {
		display: grid;
		grid-template-columns: repeat(auto-fill, var(--p151-card-width, 5.85rem));
		gap: 0.45rem;
		justify-content: start;
		align-items: stretch;
	}

	.pokedex-grid--flat .p151-member {
		width: 100%;
		min-width: 0;
		max-width: none;
		flex: 1 1 auto;
		min-height: 100%;
	}

	.pokedex-grid--chains {
		display: flex;
		flex-wrap: wrap;
		gap: 0.45rem;
		align-items: stretch;
	}

	.p151-evoline .p151-member:hover,
	.p151-evoline .p151-member:focus-visible {
		background: color-mix(in srgb, var(--p151-chain-type) 12%, var(--color-surface) 88%);
	}

	.p151-member__art {
		margin: 0 auto;
		width: calc(3.35rem + 5px);
		height: calc(3.35rem + 5px);
		object-fit: contain;
	}

	.p151-member__no {
		display: block;
		font-size: 0.58rem;
		font-weight: 700;
		color: var(--color-muted);
		line-height: 1.2;
		font-variant-numeric: tabular-nums;
	}

	.p151-member__label {
		font-weight: 600;
		font-size: 0.7rem;
		line-height: 1.3;
		word-break: break-word;
		display: -webkit-box;
		-webkit-box-orient: vertical;
		-webkit-line-clamp: 2;
		overflow: hidden;
	}

	.p151-member__en {
		font-size: 0.6rem;
		color: var(--color-muted);
		line-height: 1.2;
		word-break: break-word;
	}

	.p151-member__types {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 0.22rem;
		line-height: 1.2;
	}

	.pokedex-type-chip {
		display: inline-block;
		padding: 0.1rem 0.38rem;
		border-radius: 0.25rem;
		font-size: 0.72rem;
		font-weight: 700;
		line-height: 1.35;
		color: #fff;
		background: color-mix(in srgb, var(--type-color) 88%, #000 12%);
		text-shadow: 0 1px 0 color-mix(in srgb, #000 28%, transparent);
	}

	.pokedex-type-chip--weak {
		font-size: 0.66rem;
		font-weight: 600;
		padding: 0.06rem 0.32rem;
	}

	.p151-member__weak {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		align-items: center;
		gap: 0.18rem;
		line-height: 1.2;
	}

	.p151-weak-prefix {
		font-size: 0.58rem;
		font-weight: 600;
		color: var(--color-muted);
	}

	.p151-panel--hide-names .p151-member:not(.p151-member--revealed),
	.p151-member--mask-name:not(.p151-member--revealed) {
		cursor: pointer;
	}

	.p151-panel--hide-names .p151-member:not(.p151-member--revealed) .p151-member__label,
	.p151-member--mask-name:not(.p151-member--revealed) .p151-member__label {
		color: transparent;
		user-select: none;
		background: color-mix(in srgb, var(--color-muted) 22%, transparent);
		border-radius: 0.22rem;
		min-height: 1.15em;
	}

	.p151-panel--hide-names .p151-member:not(.p151-member--revealed) .p151-member__en,
	.p151-member--mask-name:not(.p151-member--revealed) .p151-member__en {
		display: none;
	}

		@media (max-width: 639px) {
		.p151-grid {
			display: grid;
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 0.35rem;
		}

		.pokedex-grid--chains {
			display: flex;
			flex-direction: column;
			gap: 0.4rem;
		}

		.pokedex-chain-group__track {
			flex-wrap: wrap;
		}

		.p151-toolbar__toggle {
			font-size: 0.78rem;
			padding: 0.5rem 0.65rem;
		}

		.p151-search input {
			font-size: 0.86rem;
		}

		.p151-evoline {
			display: grid;
			align-items: stretch;
			gap: 0.18rem 0.12rem;
			min-width: 0;
			padding: 0.35rem 0.28rem 0.32rem;
		}

		.p151-evoline[data-count='1'] {
			grid-template-columns: minmax(0, 1fr);
		}

		.p151-evoline[data-count='2'],
		.p151-evoline[data-count='3'],
		.p151-evoline[data-count='4'] {
			grid-column: 1 / -1;
		}

		.p151-evoline[data-count='2'] {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.p151-evoline[data-count='3'] {
			grid-template-columns: repeat(3, minmax(0, 1fr));
		}

		.p151-evoline[data-count='4'] {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.p151-evoline__sep {
			display: none;
		}

		.p151-member {
			padding: 0.22rem 0.12rem 0.26rem;
		}

		.p151-member--card {
			--p151-card-width: 100%;
			--p151-card-min-height: 7.85rem;
			width: 100%;
			min-width: 0;
			max-width: none;
			flex: 1 1 auto;
		}

		.pokedex-grid--flat {
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 0.35rem;
		}

		.pokedex-flat-cell {
			min-height: var(--p151-card-min-height, 7.85rem);
		}

		.p151-member__art {
			width: calc(2.65rem + 5px);
			height: calc(2.65rem + 5px);
		}

		.p151-member__no {
			font-size: 0.54rem;
		}

		.p151-member__label {
			font-size: 0.62rem;
		}

		.p151-member__en {
			display: none;
		}

		.p151-member__types {
			gap: 0.16rem;
		}

		.p151-member__weak {
			gap: 0.12rem;
		}
	}

	@media (min-width: 640px) {
		.p151-member {
			flex: 0 0 5.85rem;
			width: 5.85rem;
			min-width: 5.85rem;
		}

		.p151-member__art {
			width: calc(3.5rem + 5px);
			height: calc(3.5rem + 5px);
		}

		.p151-member__label {
			font-size: 0.72rem;
		}

		.p151-member__en {
			font-size: 0.62rem;
		}
	}

	@media (min-width: 1100px) {
		.p151-grid {
			gap: 0.5rem;
		}

		.p151-member:not(.p151-member--card) {
			flex: 0 0 5.5rem;
			width: 5.5rem;
			min-width: 5.5rem;
		}

		.p151-member:not(.p151-member--card) .p151-member__art {
			width: calc(3.2rem + 5px);
			height: calc(3.2rem + 5px);
		}
	}
</style>
